## Naming conventions

1. Every data structure and function defined within the project
   must stay in the Homunculus:: namespace.

2. Every preprocessor directive defined within the
   project must have the HMC_ prefix.

3. 